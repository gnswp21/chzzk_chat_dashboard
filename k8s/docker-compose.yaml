services:
  client:
    container_name: client
    build: .
    volumes:
      - .:/app
    working_dir: /app
    command: ["sleep", "infinity"]
    environment:
      - EKS_NAME=chzzk-eks-cluster

  
